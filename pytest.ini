# Architecture conformance tests live at repo root.
# Keep de/tests as default for DE package runs; invoke architecture suite explicitly:
#   pytest tests/architecture -q
[pytest]
asyncio_mode = auto
testpaths = de/tests
pythonpath = .
