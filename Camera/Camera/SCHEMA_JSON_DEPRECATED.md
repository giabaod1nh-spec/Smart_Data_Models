# DEPRECATED — NOT SOURCE OF TRUTH

This `schema.json` is a **duplicate of the upstream Camera schema** and does **not** describe the Camera / Intersection / TrafficLight models used by this project.

**Source of Truth for RT-DE Contract v1.0.0:**

1. `*_model.yaml` in this package (domain model)
2. Repository root `contracts/` (RT ↔ DE interface)

Do not use this file for contract freeze, validation, or DE parsers.
