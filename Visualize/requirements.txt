# SUMO TraCI backend (Visualize/)
# traci itself ships with SUMO (SUMO_HOME/tools) — not installed via pip.
requests>=2.28.0
pytest>=7.0.0
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
psutil>=5.9.0
