<!-- Allow de-webhook (Docker network) to reach ClickHouse as default user (dev stack). -->
<clickhouse>
    <users>
        <default>
            <networks replace="replace">
                <ip>::/0</ip>
            </networks>
        </default>
    </users>
</clickhouse>
